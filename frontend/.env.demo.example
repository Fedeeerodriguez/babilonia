# ============================================================
#  ENTORNO DEMO — frontend (NO usar en producción real)
#  Apunta al backend demo. Se usa al BUILDEAR el frontend (Vite inyecta en build).
# ============================================================

# URL pública del backend demo (la que expongas en EasyPanel/Cloudflare)
VITE_API_URL=https://api-demo.babilonia.tudominio.com
